<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Items</title>
    <link rel="stylesheet" href="/styles.css"> <!-- Link to your CSS file -->
</head>
<body>


<div class="item-container">
    <div class="item-card">
        <img src="<%= data.image %>" alt="<%= data.title %>" class="item-image">
        <div class="item-details">
            <h3 class="item-title"><%= data.title %></h3>
            <span class="item-subject"><%= data.subject %></span>
            <span class="availability <%= data.quantity === 0 ? 'unavailable' : 'available' %>">
                <%= data.quantity === 0 ? "Unavailable" : `${data.quantity} left` %>
            </span>
            <button class="view-details-button" onclick="openModal('<%= data._id %>')">View Details</button>
        </div>
    </div>
</div>

